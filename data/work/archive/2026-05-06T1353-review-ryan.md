---
area: null
contexts: []
created: 2026-05-06 13:53:43.287541
defer_until: null
due: null
energy: low
id: 2026-05-06T1353-review-ryan
order: null
output: |
  ## Agent run 2026-05-06T14:05Z

  Reviewed PR #44570 (Ryan Rogers, ENT-5945 / ENT-5932) — "ConfigurePMSIntegrationCreateConfigurationPlan: reconcile ignored_room_type_codes on re-run".

  ### Verdict
  Approve with minor questions. The fix is correct, targeted, and well-tested. Two things worth raising before merge.

  ### What the PR does
  - `ConfigurePMSIntegrationCreateConfigurationPlan.execute`: was only calling `update_account` when `not account_created and config.account_groups` was truthy. Now calls it unconditionally when the account already exists, forwarding `account_groups=config.account_groups or None` and `ignored_room_type_codes=config.ignored_room_type_codes`.
  - Threads `ignored_room_type_codes` through `PMSGatewayService.update_account` and `UpdateAccountRequest`.
  - Closes ENT-5945 (Wyndham OHIP fresh onboarding) and ENT-5932 (PMS-switch flip).

  ### What I verified
  - Gateway side (`pms-gateway/accounts/services/account.py`) already has `ignored_room_type_codes` in `ACCOUNT_PATCH_FIELDS` and `ACCOUNT_ALLOWED_FALSY_FIELDS`, so an empty list correctly clears it via PATCH while `None` is skipped — single-side fix claim is accurate.
  - `account_groups=None` semantics preserved: gateway PATCH handler at `accounts/services/account.py:172` only touches groups `if params.account_groups is not None` (`None` = leave alone, `[]` = clear). The `config.account_groups or None` guard maintains parity with pre-PR behavior of "no groups → don't touch".
  - `PmsIntegrationConfig.ignored_room_type_codes: list[str]` (non-Optional), so `update_account` always receives a concrete list — no surprise None values.
  - Cache: `update_account` does NOT clear `_GatewayAccountCache`, but the cache isn't populated until the post-run check calls `get_account(use_cache=True)`, so the check sees fresh state. No new cache regression introduced.
  - Tests cover: existing-account-with-codes (regression for ENT-5945), empty-account-groups-passes-None to gateway, ignored_room_type_codes empty-list-vs-None distinction at the gateway service layer.

  ### Worth raising before merge

  1. **Scope creep on the unconditional update.** Previously `update_account` was gated on `config.account_groups` being truthy. Now every re-run on an existing account will also reconcile `name`, `is_strategic_account`, `is_demo`, `hotel_uuid`, `currency`, `timezone` (whatever's currently in `UpdateAccountRequest`). Probably benign / arguably more correct (gateway should mirror canonical Hotel state), but the PR description doesn't call it out. Worth Ryan confirming this is intended and that no consumer relies on those fields being frozen post-create.

  2. **Empty-list-as-clear edge case.** If any ConfigProvider sets `ignored_room_type_codes=[]` while the gateway has manually-set codes, this PR will clear them on re-run. Same semantics already applied at create time (`get_or_create_account` passes `(ignored_room_type_codes or [])`), and the post-run check was already complaining about divergence — so consistency-wise this is a fix, not a regression. But worth a quick scan of brand ConfigProviders to confirm none rely on "set once at create, never touch again".

  ### Nits (non-blocking)
  - `account_groups=config.account_groups or None`: works correctly but slightly cryptic. `config.account_groups if config.account_groups else None` reads more obviously. Not worth a re-push.
  - The renamed test `test_execute__updates_account_with_no_groups_passes_none` is clearer than the original — good rename.

  ### No PR comment posted — review is for Gareth's eyes only per task setup.
project: null
source_id: null
tags: []
time_minutes: 5
title: Review ryan
updated: 2026-05-06 14:05:00.000000
waiting_on: null
waiting_since: null
working_on: false
---

https://github.com/canary-technologies-corp/canary/pull/44570/changes