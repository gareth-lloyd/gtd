---
area: null
completed_at: 2026-09-09 12:47:19.717966
contexts:
- react
created: 2026-08-14 07:10:44.687998
defer_until: null
due: 2026-09-02
energy: medium
id: 2026-08-14T0710-prioritise-and-assign-ent-7201-consistency-rules-c
order: null
output: |-
  ## Agent run 2026-09-09T12:40:00

  **Bottom line: the premise has moved. This is no longer "a ticket to prioritise" — it is a live dependency of SDM's Q3 deliverable, and half of it is already being fixed by someone else.** Recommendation: descope, mark High, assign to Luiza Manhães (SDM), keep the Problem-2 design call for yourself. Nothing written to Linear — draft edits below need your OK.

  ### What changed since you filed it (13 Aug)

  The SDM project is **"SDM Oncall Efficiency: Config Linter, Config Cleanup, Brand Profiles, Support KB"** — lead luiza manhaes, status **Implementation**, 2026-08-14 → **2026-09-29** (20 days out).
  https://linear.app/canary-technologies/project/sdm-oncall-efficiency-config-linter-config-cleanup-brand-profiles-eb744cdaff74

  "Plans to build on the same decorator next block" is stale. They are building on it **now**, and shipping:

  | Issue | Status | What it does to the decorator |
  | --- | --- | --- |
  | [SDM-4989](https://linear.app/canary-technologies/issue/SDM-4989/config-linter-reconcile-with-gareths-draft-42860-and-verify-rule) | In Review | Lands your draft #42860's rules, rebased — [PR #55352](https://github.com/canary-technologies-corp/canary/pull/55352) |
  | [SDM-4990](https://linear.app/canary-technologies/issue/SDM-4990/config-linter-authorization-consistency-rules-batch-1) | In Review | Authorization consistency rules, batch 1 |
  | [SDM-4992](https://linear.app/canary-technologies/issue/SDM-4992/config-linter-surface-rules-from-authorizationconfiguration-admin) | In Review | [PR #54997](https://github.com/canary-technologies-corp/canary/pull/54997) — registers 4 *dormant* card-posting checkers (same bug class) |
  | [SDM-4993](https://linear.app/canary-technologies/issue/SDM-4993/config-linter-run-the-config-health-check-at-demolive-flip-warn-only) | In Review | Runs the health check at demo→live flip |
  | [SDM-4991](https://linear.app/canary-technologies/issue/SDM-4991/config-linter-gateway-cant-do-auth-only-as-a-consistency-rule-over-pms) | In Progress | "gateway can't do auth-only" rule |

  All assigned to Luiza. All parented under [SDM-4862](https://linear.app/canary-technologies/issue/SDM-4862/config-linter-first-authorization-rule-set-in-the-enterprise-rules).

  ### Scope 3 and 4 are already done — by Luiza, in PR #55352

  Her diff on `check_in/configuration_rules/consistency_rules.py` swaps both dead strings:
  - `hotel.check_in_configuration.tokenize_card` → `...is_tokenizing_with_hotel_payment_gateway`
  - `hotel.payment_gateway_config_id` → `hotel.check_in_configuration.payment_gateway_config_id`

  ...and rewrites the tests. **Leave scopes 3 and 4 in ENT-7201 and it will read as duplicate work.** Note she did *not* implement the legacy-gateway exemption (the `hotel.payment_gateway.gateway` / FreedomPay-Shift4 skip from `CheckInConfiguration.clean()`) — that is a real gap worth a comment on her PR, and it is the one piece of scope 3 that survives.

  ### Both real problems are still fully open on master

  Verified against master today:
  - **Problem 1 (class, not instance).** `configuration_consistency_rule` at `rules_based_configuration/services/consistency.py:18-21` is a bare `_RULES.append(func)`. No key declaration, no validation. Rules read `dict[str, Any]`, so a misspelled key is a silent no-op forever.
  - **Confirmed the rule was dead:** `tokenize_card` appears nowhere in the codebase outside that one rule.
  - **Problem 2.** `ANY_NON_NULL` is still the singleton sentinel (`conformity.py:32-46`). Every presence rule still silently passes in the tree/CI layer.

  **Problem 2 is now actively harmful, not theoretical.** Luiza's new rules are overwhelmingly presence-shaped — `is not None`, `!= "disabled"`. Examples from #55352: `precheckin_method_requires_auto_post`, the addons `integration_enabled` family, and the repointed payment-gateway rule. Every one of them will report green in the CI layer regardless of hotel state. SDM is on track to ship a linter that reduces oncall load by asserting nothing, and to trust it.

  ### The fix is much cheaper than the ticket says — worth rewriting scope 1

  The ticket proposes runtime validation against `get_all_fields()` at registration/finalize. Not needed. **A statically-checked, CI-enforced keyspace already exists:**
  - `rules_based_configuration/services/conformity.pyi` defines a `SettingKey` Literal union of every valid key.
  - It is generated by `manage.py generate_setting_types` (`setting_type_generator.py`).
  - Staleness is **already a CI gate**: `generate_setting_types --check` at `.github/workflows/canary_back_end_django.yml:227`.
  - `GroupAttributes.define()` already gets static key validation from it via `@overload`.

  Consistency rules bypass all of this purely because they take `dict[str, Any]`. So scope 1 becomes: `@configuration_consistency_rule(keys=[...])` typed as `Sequence[SettingKey]`. Pyrefly then rejects a misspelled key **at typecheck time, before merge** — strictly better than the runtime/startup failure the ticket asks for, and no new machinery. Estimate drops to ~2 points.

  ### Answering Andrea's capacity question directly

  Her [comment](https://linear.app/canary-technologies/issue/ENT-7201#comment-20df1cc5-299d-47a0-8a4f-213e52f016eb) — "not sure we will have capacity given the IHG priority" — is answerable now: **this should not consume Enterprise engineering capacity at all.**

  I checked Enterprise's In Progress board. Nobody is free: Andrés Figueira on Wyndham segmentation (ENT-7404..7408), Lautaro Mena on SSO, Ryan Rogers on Content Gateway, Ishwar Nagar + Andrea on IHG scripting. Also relevant — [SDM-4996](https://linear.app/canary-technologies/issue/SDM-4996/brand-profiles-template-named-config-package-co-build-with-enterprise) records Enterprise's Configuration Drift Detection project as Paused since 8/21, "on IHG pilot-to-scale through at least 9/1" (that date has now passed).

  The work belongs with whoever is already in the file — Luiza — with you owning the one design call. That reframes the ask from "Enterprise polishes its own engine" to "make SDM's Q3 deliverable trustworthy", which is a much easier yes.

  ### Recommendation — draft Linear changes, NOT applied

  1. **Priority: High (2).** Justification for the field: it gates the correctness of SDM's config linter, which is in Implementation with a 9/29 target and ~10 rules already in review.
  2. **Assignee: luiza manhaes.** She owns the consumer, is in the file this week, and has the two open PRs. You review as engine owner.
  3. **Status: Triage → Todo**, and add relation **ENT-7201 blocks SDM-4862**. This is the structural fix — it makes the dependency visible instead of relying on the "worth doing because…" sentence Andrea pushed back on.
  4. **Descope:** cut scope 4 entirely and cut scope 3 down to just the legacy-gateway exemption (leave a note that #55352 did the rest). Retitle to match what is left, e.g. *"Consistency rules: validate declared keys statically and decide ANY_NON_NULL semantics"*.
  5. **Rewrite scope 1** along the `Sequence[SettingKey]` lines above.
  6. **Project:** it currently sits in *Eng Improvements* (lead Andrea, target date 2026-08-12 — already lapsed; it is a parking lot). Given it now blocks SDM, either move it to the SDM project or leave it and rely on the blocks relation. Blocks relation is the cheaper signal.

  ### Your call before anything ships

  - **Scope 2 is yours and it is the real decision** — sentinel-aware skip vs. layer-tagging vs. documenting presence rules as fleet-health-only. The ticket recommends sentinel-aware skip and I agree, but Luiza should not guess at this while landing rules that depend on the answer. This is ~30 minutes of your time and it unblocks her, so it is the highest-leverage thing in this whole item.
  - Do you want me to post any of the above to Linear (priority, assignee, status, relation, description rewrite) or comment on PR #55352 about the missing legacy-gateway exemption? **I have written nothing** — all of it needs your explicit go-ahead.

  ## Agent run 2026-09-09T09:47:00 — edits APPLIED to Linear

  User approved ("make all edits"). All six changes written to [ENT-7201](https://linear.app/canary-technologies/issue/ENT-7201/consistency-rules-validate-declared-keys-statically-and-decide-any-non) and verified by re-read:

  | Field | Was | Now |
  | --- | --- | --- |
  | Title | "...`payment_gateway_required_for_tokenization` is dead and proves it" | "Consistency rules: validate declared keys statically and decide ANY_NON_NULL semantics" |
  | Priority | No priority | **High (2)** |
  | Assignee | unassigned | **luiza manhaes** |
  | Status | Triage (since 13 Aug) | **Todo** |
  | Relation | none | **blocks SDM-4862** |
  | Description | 4 scopes, half stale | rewritten — see below |

  Note the URL slug changed with the title; the old link still redirects.

  **Description rewrite — what was cut:** the "rule is dead / 5-minute change" framing, old scope 4 (fix tests), most of old scope 3, and "SDM plans to build on this next block". All superseded by [PR #55352](https://github.com/canary-technologies-corp/canary/pull/55352).

  **What was kept and sharpened:** Problem 1 reframed from instance to bug class, with the `SettingKey`/`conformity.pyi` finding that makes scope 1 a typing change caught by pyrefly pre-merge rather than runtime validation. Problem 2 kept, with corrected line refs (`conformity.py:653-679`, `checks.py:8-25` — the old `665-676`/`checks.py:19` had drifted) and the note that it blocks rules already in review. Scope 3 reduced to just the legacy-gateway exemption, framed as a gap in #55352.

  **Side effect:** Linear auto-linked SDM-4989 as a "related to" relation because the description mentions it. Harmless, but it wasn't one of the six deliberate changes.

  ### Still open — not done, still yours

  * **Scope 2 is the bottleneck and it is your decision**, not Luiza's: sentinel-aware skip vs. layer-tagging vs. documenting presence rules as fleet-health-only. The ticket now says in bold that it blocks the SDM rules in review, so she will come asking. ~30 min of your time.
  * **No comment posted on PR #55352** about the missing legacy-gateway exemption — the ticket now records it as scope 3, but Luiza has no signal on her actual PR. Worth a line there.
  * **Andrea has not been told.** She asked the priority question on 13 Aug and the answer changed materially (it no longer costs Enterprise capacity). She will see an unexplained Triage→Todo/High on a ticket she pushed back on. A short reply on her comment thread would close that loop.
  * **Project is still *Eng Improvements*** (target date lapsed 2026-08-12). Left deliberately — the blocks relation carries the signal more cheaply than a project move.

  ## Agent run 2026-09-09T09:55:00 — crossover check + ticket moved teams

  ### The ticket is now SDM-5039, not ENT-7201

  Same UUID (`ad2d8c27…`), so it was **moved**, not copied. State history shows Todo at 09:45:21 (my write), then SDM Triage at 09:46:08 — ~47s later, by someone else. New URL: https://linear.app/canary-technologies/issue/SDM-5039/consistency-rules-validate-declared-keys-statically-and-decide-any-non

  The move reverted two of my six edits: **priority High → Medium**, **status Todo → SDM Triage**. Project → SDM Oncall Efficiency, `backend` label dropped. Title, description, assignee (Luiza), and `blocks SDM-4862` all survived. Left as-is — the move looks deliberate and it lands the ticket where the work is.

  ### Crossover with the SDM config-linter project

  **1. SDM-4990 already encodes scope 1 as manual discipline.** Its constraints read: *"Every key must exist in `rules_based_configuration/conformity.pyi` (71 `hotel.authorization_configuration.*` keys are typed there)"*. Joshua found the same keyspace independently. Scope 1 converts that hand-checked rule into a machine-checked one — complementary, and evidence the constraint is already being leaned on.

  **2. SDM-4993 partly answers scope 2 — read it before deciding.** [PR #55050](https://github.com/canary-technologies-corp/canary/pull/55050), In Review. It wires `HotelConfigHealthService.check(hotel)` into the demo→live flip. That service runs `_check_consistency(hotel, …)` against a **real hotel's settings** — no sentinel. So there are three layers, not two: tree/`finalize()`, the Django system check, and per-hotel health. Presence rules are meaningful only in the third. Scope 2's "tag rules with the layers they are valid in" option is therefore not hypothetical — SDM is building that third layer right now.

  **3. ENT-7103 collides with scope 1 on file ownership.** [PR #55727](https://github.com/canary-technologies-corp/canary/pull/55727) (Tincho, In Review) touches `conformity.pyi`, `setting_type_generator.py` and `conformity.py` — the exact generated-stub machinery scope 1 depends on. It adds `explanation=` to `ConfigSpec`/`.define()`. SDM-4990 already cites it as a blocker ("`ConfigurationConsistencyError` has no severity/explanation field yet"). **Land ENT-7103 first**, or expect a stub regeneration conflict.

  **4. ENT-7113 is the counterweight nobody has linked.** [ENT-7113](https://linear.app/canary-technologies/issue/ENT-7113/scope-us-centric-final-rules-by-regioncapability) (Tincho, Enterprise, Backlog) documents that the FINAL tree rule mandating `is_tokenizing_with_hotel_payment_gateway = True` + non-null `payment_gateway_config_id` false-flags **154 live Wyndham hotels** — overwhelmingly international (Mexico 58, Canada 35, Argentina 8…), all Opera Cloud, tokenizing via the PMS-native tokenizer or raw Stripe.js. I confirmed that path is first-class in `check_in/services/check_in_payment.py`: the `check_in_tokenizes_via_stripe_gateway` docstring names both the PMS-native tokenizer and the raw-card Stripe.js flow as legitimate.

  Those are **the same two keys** #55352 just repointed the consistency rule onto. Two consequences:

  * Scope 3 in the ticket names only the legacy FreedomPay/Shift4 exemption. **PMS-native tokenization is a second, larger, already-quantified exemption class** the ticket doesn't mention.
  * ENT-7113 changes what the tree *declares* for those keys; scope 2 changes how consistency rules *read* declarations. Same keys, opposite sides, different owners (Tincho/Enterprise vs Luiza/SDM), no link between the tickets.

  *Caveat:* the consistency rule fires only when the tokenize flag is `True`, so it probably won't false-positive on those 154 as written. This is a coordination risk, not a live bug — but the two fixes must agree.

  **5. ENT-7113 §2 overlaps a rule already in #55352.** It documents the auto-post family false-flagging arrival-only / non-2-way hotels where posting is a no-op — the same false-positive class as Luiza's new `precheckin_method_requires_auto_post`, one layer up.

  **6. SDM-4992 is the same bug class in a different registry.** The four dormant checkers in `hotels/utils/pms_gateway_config_checks.py` (`FEATURE_FLAG_CHECKERS`) are checks that were never registered and so never ran. Scope 1 fixes the consistency-rule registry only. Pattern repeats; not worth widening scope.

  ### Suggested follow-ups — none applied

  * Link **ENT-7113 ↔ SDM-5039** (related). Nobody has connected them and they touch the same keys from opposite directions.
  * Sequence: **ENT-7103 lands before scope 1.**
  * Add PMS-native tokenization to scope 3's exemption list.
  * Re-set priority to High if the SDM-team move to Medium wasn't deliberate — worth asking Joshua/Luiza rather than reverting.
project: 2026-04-16T1210-unblock-team
source_id: https://linear.app/canary-technologies/issue/ENT-7201/consistency-rules-cant-express-null-presence-invariants-payment
tags:
- morning-gtd
- linear
time_minutes: 15
title: 'Prioritise and assign ENT-7201: consistency rules can''t express null-presence
  invariants'
updated: 2026-09-09 12:50:54.686521
waiting_on: null
waiting_since: null
working_on: false
---

I filed it after telling Joshua Hart in #epd-enterprise "we'll fix that". Still sitting in Triage with no priority or assignee, and SDM's oncall-improvement project plans to build on the same decorator next block.
https://linear.app/canary-technologies/issue/ENT-7201/consistency-rules-cant-express-null-presence-invariants-payment