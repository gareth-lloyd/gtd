---
area: null
contexts: []
created: 2026-06-04 08:06:58.370459
defer_until: null
due: null
energy: low
id: 2026-06-04T0806-review-rami
order: null
output: |
  ## Agent run 2026-06-04T10:24Z — Review of PR #47037 [TOOL-200]
  "Share one rule source for single and bulk product availability" by Ramiro Nieto
  https://github.com/canary-technologies-corp/canary/pull/47037
  (Static review only — did NOT post to GitHub, did NOT run the PR's test suite
  locally since my checkout is on a different worktree branch. Verdict below is
  from reading the diff + surrounding code + git history.)

  ### Verdict: Solid, well-scoped refactor. Approve in spirit, but ONE
  important pre-merge blocker (stale base / TEAM_CHAT) to resolve first.

  ### What it does (and does well)
  - Collapses the duplicated availability logic: `get_union_of_..._for_hotels`
    no longer re-derives queryset filters; it now iterates the same
    PRODUCT_ACTIVATION_RULES and calls a new `is_active_for_any(hotel_ids)`
    bulk predicate. This is the right fix for the drift the ticket describes.
  - `_hotel_field_rule` helper derives BOTH the single-hotel attr access and
    the bulk queryset filter from one `field_lookup` string, so simple
    boolean-field products structurally cannot disagree. Nice.
  - Making `is_active_for_any` a required (non-default) dataclass field is a
    good safety net: every rule MUST supply a bulk predicate or import fails.
  - Fixes the real bug the ticket flagged: SALES_BOOKINGS was missing from the
    bulk path and is now included. Behavior change is intended/confirmed.
  - Tests added are the right ones: bulk SALES_BOOKINGS, contracts-without-
    rollout exclusion, and a union==single-hotel invariant.

  ### BLOCKER — stale base, TEAM_CHAT will regress on merge (HIGH)
  The PR branch's merge-base (6621270, 2026-06-02 16:52Z) is ~15 min OLDER than
  the Team Chat commit aa7bf045f84 "[MSG-4951] Team Chat foundation" (2026-06-02
  17:07Z). Confirmed: `git merge-base --is-ancestor aa7bf045f84 <PR>` => NO.
  - Current master added CanaryProducts.TEAM_CHAT to the enum, to
    get_available_products' rule list, AND to the bulk union dict
    (team_chat_enabled).
  - This PR rewrites the exact two regions master touched and has NO TEAM_CHAT
    (its enum stops at ...STORE, TABLET_EFOLIO, TABLET_REGISTRATION, UPSELLS,
    VOICE; no team_chat anywhere).
  - On rebase onto master there WILL be a conflict in PRODUCT_ACTIVATION_RULES
    and in the union method. The resolver must keep TEAM_CHAT and give it a
    bulk predicate, i.e.
      _hotel_field_rule(CanaryProducts.TEAM_CHAT, "team_chat_enabled",
                        "team chat enabled is set to True on the hotel.")
  - Good news: the existing drift-guard test
    `test_rules_cover_every_product_previously_returned...` already lists
    TEAM_CHAT in its expected set, AND the now-required `is_active_for_any`
    field means a TEAM_CHAT rule built without a bulk predicate is a TypeError
    at import. So a careless rebase fails loudly rather than silently — but the
    author still needs to do the rebase deliberately. Worth a PR comment.

  ### Minor notes (non-blocking)
  - Result ORDER of the union changed from dict-insertion order to
    PRODUCT_ACTIVATION_RULES order. Checked both production callers
    (portfolio/portfolio_hotels_products.py:41 and
    user/user_portfolio_hotels.py:70) — both just do `[p.value for p in ...]`
    into a JSON `data` array, treated as a set client-side. No risk, but flag
    if the frontend ever sorts-by-arrival.
  - `_hotel_field_rule.is_active` now returns `bool(value)` vs the old lambdas
    returning the raw attribute. Equivalent under the truthy `if rule.is_active`
    usage; arguably cleaner. No concern.
  - Classmethod-as-predicate (`is_active_for_any=CanaryProducts._get_*_many`)
    works because accessing a classmethod on the class yields a cls-bound
    method — same pattern already proven for `is_active`. Fine.
  - SALES_BOOKINGS single vs bulk: single uses `hotel.authorization_configuration`
    (AutoOneToOne — can create a row on read; pre-existing, unchanged), bulk uses
    an INNER JOIN `authorization_configuration__has_contract_v3_rollout=True`.
    For a single hotel they agree (no config row => both False). Consistent.
  - The union==single invariant test only exercises the sales_bookings fixture;
    it does not cover the AUTHORIZATIONS-via-free-auths or COMPENDIUM
    single-vs-bulk equivalence (those bulk/single predicates are independent
    implementations, pre-existing). Not introduced by this PR; could be a nice
    follow-up but not required here.

  ### Recommendation
  Approve the design. Ask Ramiro to rebase onto current master and confirm
  TEAM_CHAT is re-added as a shared `_hotel_field_rule(..., "team_chat_enabled")`
  before merge, then re-run the product test files. No code changes needed
  beyond the rebase.
project: null
source_id: null
tags: []
time_minutes: 5
title: review rami
updated: 2026-06-04 15:44:25.028299
waiting_on: null
waiting_since: null
working_on: false
---

https://github.com/canary-technologies-corp/canary/pull/47037