---
area: null
contexts: []
created: 2026-06-08 14:44:19.434388
defer_until: null
due: null
energy: low
id: 2026-06-08T1444-review-ryan
order: null
output: |
  ## Agent run 2026-06-08T16:05Z

  Reviewed Ryan's two stacked PRs from the Slack DM, using the pr-review-toolkit
  agents (code review, tests, comments, silent-failure) against fresh worktrees of
  each PR branch. Both are part of the Pre-Production Hotels work
  (ENT-6171 / ENT-6176). #46618 is the base (on master); #46621 stacks on it.
  Ryan asked to review #46618 first. NOTE: I did NOT post anything to GitHub — these
  are notes for you to act on / relay. Nothing is a hard bug-bug, but there is one
  real correctness item on #46621 worth raising before merge.

  ====================================================================
  PR #46618 — ENT-6171: staging portfolio identifiers, mappings, brand-mapping
  https://github.com/canary-technologies-corp/canary/pull/46618  (6 files, +104)
  ====================================================================
  Verdict: No correctness blockers. Core wiring is correct and well-tested. Main
  thing to resolve before merge is two DEAD constants whose comments overclaim.

  Important / worth raising:
  1. TWO UNUSED CONSTANTS WITH MISLEADING COMMENTS (grep-confirmed, zero consumers
     anywhere in repo):
     - STAGING_PORTFOLIO_IDENTIFIERS (hotels/models/portfolio.py:106). Comment says
       "Used to gate GrowthBook features and exclude from analytics." FALSE today —
       nothing reads it. The only staging analytics exclusion that exists keys off
       hotel.slug_name.startswith("staging-") (analytics/.../amplitude_hotel_group_sync.py),
       unrelated to this frozenset.
     - PRODUCTION_TO_STAGING (portfolios/services/portfolio.py:35). Comment says
       "Consulted ONLY by the staging onboarding command" — that command isn't in this
       PR, so the claim describes a caller that doesn't exist yet.
     Ask Ryan: are the consumers coming in a follow-up chunk? If so, either (a) reword
     the comments to mark them as intent/not-yet-wired, or (b) land each constant with
     its first consumer. As written they read as live behavior that isn't there.
  2. Per-domain wiring gap to confirm consciously: get_membership_program_redirect_url
     (guest/services/membership_program.py:~302) gates directly on WYNDHAM/BEST_WESTERN/
     IHG_PILOT and is NOT backfilled by the loyalty priority map, so staging hotels get
     no enrollment redirect URL. Consistent with the PR's "wire staging explicitly per
     domain" philosophy and probably fine for UAT hotels — but it's exactly the
     "omitting staging id is a latent bug" class. Worth a one-line confirmation that
     staging hotels aren't expected to hit that path. (PR body already says booking
     rate codes + segmentation get wired in Chunk 3.)

  Suggestions:
  - Test gap (low): the staging loyalty priority entries are exercised only via the
    canonical-map table test, not through the real get_program_identifier_for_hotel
    resolution path — so the priority= value on the new entries is untested. One added
    test (hotel in WYNDHAM_STAGING resolves to LoyaltyProgramIdentifier.WYNDHAM) closes it.
  - Enum casing: new values are lowercase ("wyndham_staging","ihg_staging",
    "best_western_staging") while some prod brands are uppercase ("BW","IHG"). Harmless
    (CharField has no choices= binding, no migration), but note the literal is
    best_western_staging (not BW_STAGING) when authoring GrowthBook targeting rules.

  Strengths: mutual-exclusion logic (staging in same EXCLUSIVE_PORTFOLIOS set as prod
  brand) is correct and has solid behavioral tests incl. negative assertions (hotel NOT
  added to prod portfolio on conflict). Loyalty resolution works end-to-end for staging.
  Adding staging ids to PUBLIC_PORTFOLIO_IDENTIFIERS (GrowthBook targeting) is correct.

  ====================================================================
  PR #46621 — ENT-6176: onboard_staging_hotel_from_production_hotel + SF write guard
  https://github.com/canary-technologies-corp/canary/pull/46621  (2 files, +293)
  ====================================================================
  Verdict: Strong PR; the SF read-only guard is genuinely sound. ONE correctness item
  to raise before merge, plus a couple of comment fixes.

  CRITICAL (verified directly, not just an agent claim):
  1. The only callable staging input crashes with a bare KeyError.
     WYNDHAM_STAGING_MSA is the sole member of STAGING_ONBOARDING_TYPES, so it passes
     the guard at onboarding.py:465 — but it is NOT a key in ONBOARDING_TYPE_CONFIG
     (grep-confirmed: zero "staging" entries in that dict). Line 486
     `config = ONBOARDING_TYPE_CONFIG[staging_onboarding_type]` then raises an unguarded
     KeyError, not the typed OnboardingServiceError the rest of the method uses. All 4
     new tests pass only because they INJECT a config entry and restore it in finally —
     no test exercises the real (unwired) config, so the gap is fully masked.
     Most likely "config wiring lands in a later chunk (T8/T9)" — but as it stands the
     method is uncallable in prod. Ask Ryan to either (a) state explicitly it's dead
     until the follow-up, or (b) convert the lookup to .get() + typed
     OnboardingServiceError so the only callable input fails helpfully, not with KeyError.

  Important:
  2. _enforce_read_only_salesforce permanently mutates the caller's shared sf object
     (sf.session.request = read_only_request) and never restores it. The guard itself is
     correct & robust (verified against vendored simple_salesforce 1.12.5: query, SObject
     create/update/delete, bulk, and metadata ALL route through the same shared
     session.request, so writes raise). But the side-effect is permanent and unguarded —
     if a caller reuses that sf instance after onboarding a staging hotel, every later
     write raises far from this code. The helper returns sf as if it were a guarded copy,
     but it's the same mutated object. Suggest documenting loudly on the method (not just
     the helper) that the passed sf is permanently write-neutered, or restore in a finally
     / wrap a copy.
  3. Guard safety is version-coupled and untested against a real client. The unit test
     uses a hand-built SimpleNamespace, so a future simple_salesforce upgrade adding a
     session.post shortcut or POST-for-read endpoint would silently defeat the guard with
     no failing test. Cheap mitigation: pin/comment the lib version next to
     _SALESFORCE_READ_METHODS, and/or a test against a real Salesforce(session=fake).
  4. PR DESCRIPTION IS STALE: it lists signature
     (production_hotel, staging_onboarding_type, sf, plan_factories, running_user) but the
     actual code is (production_hotel, staging_onboarding_type, script_type, sf,
     running_user) — no plan_factories param (derived from config), script_type added.
     Actual signature is coherent; just update the description.

  Comment fixes:
  5. onboarding.py:548 — "has_authorizations is the only product flag defaulting to True"
     is FACTUALLY WRONG. On the Hotel model, has_alerts, has_short_links,
     has_reservations_page, is_customer, etc. also default to True. (Carried over verbatim
     from the old inline block — it was already wrong there, now propagated into the shared
     _create_blank_hotel helper.) Reword to drop the "only" claim.
  6. onboarding.py:471-474 — "no SalesforceHotelMetadata or Onboarding row is created" is
     true of the method body but stated absolutely; a future plan run via
     run_plans_on_hotel could create one. Mirror the hedging the SF-guard docstring already
     uses ("now or from a plan added later").

  Suggestions / test gaps:
  - No transaction-rollback test for the new method (the production path HAS one). A
    failing-plan test asserting Hotel.objects.count() unchanged is the highest-value add.
  - No test runs a REAL plan through the staging path (both reaching tests use empty plans
    or SetBrandingPlan w/ config_provider=None which no-ops), so plan integration (guarded
    sf + data flowing into a plan) is unverified.
  - Read-only guard case-insensitivity (lowercase "get") and HEAD are coded but untested.
  - STAGING_ONBOARDING_TYPE_BY_PORTFOLIO (onboarding.py:109) is also unused in this PR.

  Verified-good (non-issues): `# nosec B608` on the SOQL is justified (required_fields are
  code-defined constants, sfdc_id goes through format_soql binding — not attacker
  controllable); _create_blank_hotel extraction is behavior-preserving except a minor
  log-event-name/bound-context change (old "onboarding_create_or_update_hotel.creating_hotel"
  via bound log -> new "onboarding._create_blank_hotel" via module logger; grep
  observability configs if anything alerts on the old event name); SF reads correctly happen
  before transaction.atomic (mirrors production path, avoids holding a txn open over network
  I/O); not creating metadata/onboarding rows is safe at the orchestration layer
  (run_plans_on_hotel reads neither).

  ====================================================================
  Suggested single message back to Ryan (NOT SENT — your call):
  ====================================================================
  "Reviewed both. #46618: solid, mutual-exclusion + loyalty wiring is correct and
  well-tested. Two nits before merge — STAGING_PORTFOLIO_IDENTIFIERS and
  PRODUCTION_TO_STAGING are currently unused but their comments say they're already gating
  analytics/GrowthBook + consulted by the staging command; either reword as not-yet-wired
  or land them with their consumers. And confirm staging hotels aren't meant to hit
  get_membership_program_redirect_url (it gates on prod ids directly). #46621: nice SF
  read-only guard. One thing to fix — WYNDHAM_STAGING_MSA passes the staging guard but
  isn't in ONBOARDING_TYPE_CONFIG, so the method KeyErrors on the only valid input today;
  either flag it as dead-until-the-config-chunk or switch to .get() + OnboardingServiceError.
  Also: the guard permanently mutates the caller's sf (document or restore), the comment
  'has_authorizations is the only flag defaulting to True' is wrong, and the PR
  description's signature is stale (no plan_factories param). Tests could use a rollback
  case + a real-plan case."
project: null
source_id: null
tags: []
time_minutes: 5
title: review ryan
updated: 2026-06-08 15:44:32.875940
waiting_on: null
waiting_since: null
working_on: false
---

https://canarytechnologies.slack.com/archives/D07MJEGGV19/p1780616041110559